<!DOCTYPE html>
<html lang="en">
  <%-include('./components/head.ejs') %>
  <body>
    <%-include('./components/header.ejs') %>

    <div class="body">
      <%#-include('./components/sidebar.ejs') %>
      <div id="content-wrapper" class="flex-box justify-content-ct">
        <div id="content">
          <h1>어서오세요 세탁특공대 입니다!</h1>
          <div class="auth-box flex-box">
            <button><a href="/login">로그인</a></button>
            <button><a href="/signup">회원가입</a></button>
          </div>
        </div>
      </div>
    </div>

    <%-include('./components/footer.ejs') %>

    <script src="/js/main.js"></script>
    <script>
      // 즉시 실행 함수에 async를 줘서 구현 => getUserInfo 함수에서 비동기 요청을함
      (async () => {
        const userInfo = await getUserInfo();
        console.log({ userInfo });
        const isLogin = !!userInfo;
        console.log({ isLogin });

        // 아래 코드는 user페이지 화면이 보였다가
        // users페이지로 이동하기에 잔상이 보임
        // 이걸 해결하고 싶으면 middleware/login-check.middleware.js 사용
        // if (isLogin) {
        //   window.location.href = "/users";
        // }

        createHeaderButton(isLogin);
        setPageInfo(userInfo);
      })();
    </script>
  </body>
</html>
